# SOREALSTORE

- Fake store application using ReactTS
- Uses routes as state
- Uses React.use for fetch & Suspense with fake caching
- No major stuff added not much responsiveness consideration other than row->col and grid auto fit